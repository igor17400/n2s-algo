from typing import List


class Solution:
	def is_odd(n: int) -> bool:
		if n % 2 == 0:
			return False
		else:
			return True

	def is_turbulent(sub_arr: List[int]) -> int:
		for k in len(sub_arr):
			# Case 1
			if is_odd(sub_arr[k])
				if sub_arr[k] > sub_arr[]

    def maxTurbulenceSize(self, arr: List[int]) -> int:
        for el in arr:
        	continue